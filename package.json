{
  "name": "claud-code-usage-monorepo",
  "private": true,
  "workspaces": [
    "web",
    "api"
  ],
  "dependencies": {
    "pg": "^8.13.1"
  },
  "scripts": {
    "build": "npm --prefix web run build",
    "build:web": "npm --prefix web run build",
    "typecheck": "npm --prefix web run typecheck"
  }
}
