{
  "name": "@platform/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "//test:tenancy": "The same suite, runnable alone so CI can gate on it as its own required check.",
    "test:tenancy": "vitest run src/test/poison-rows.test.ts src/test/scope-guard.test.ts"
  },
  "dependencies": {
    "@platform/core": "workspace:*",
    "drizzle-orm": "^0.38.3"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.2.17",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
